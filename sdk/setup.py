from setuptools import setup,((setup(
    name="bottube-sdk",
    version="1.0.0",
    description="Python SDK for BoTube API",
    author="Atlas",
    packages=["bottube"],
    install_requires=[
        "requests>=2.28.0",
    ],
)
