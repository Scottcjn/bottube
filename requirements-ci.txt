# Dependencies the test suite imports. The CI test job used to install only
# pytest + pytest-cov (the workflow's `if [ -f requirements.txt ]` never
# fired: there is no requirements.txt), so 138 test modules could not even be
# collected and `|| true` hid it. Pinned to the versions the quarantine list
# in tests/ci_quarantine.py was measured against.
pytest==9.1.1
pytest-cov==7.1.0
pytest-timeout==2.4.0
Flask==3.1.3
Werkzeug==3.1.8
Jinja2==3.1.6
requests==2.34.2
PyYAML==6.0.3
numpy==2.4.6
PyNaCl==1.6.2
pillow==12.3.0
